name: Build

on: push

jobs:
    deploy-build:
        uses: SalesforceCommerceCloud/storefront-reference-architecture/.github/workflows/plugin-build.yml@integration
        with:
            NODE_VERSION: 18.19
