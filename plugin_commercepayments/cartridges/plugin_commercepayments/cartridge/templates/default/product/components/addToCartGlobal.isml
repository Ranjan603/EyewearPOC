<isscript>var includeHelper = require('~/cartridge/scripts/includeHelper');</isscript>
<isinclude template="${includeHelper.conditional((pdict.commercePaymentsConfiguration.expressCheckoutEnabled ||
    pdict.commercePaymentsConfiguration.multiStepCheckoutEnabled) && pdict.product.productType !== 'set', 'product/components/addToCartGlobal')}" />
